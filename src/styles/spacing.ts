const baseSpacing = 10;

const spacing = {
  xss: baseSpacing * 0.2,
  xs: baseSpacing * 0.4,
  sm: baseSpacing * 0.8,
  md: baseSpacing * 1.2,
  lg: baseSpacing * 1.6,
  xl: baseSpacing * 2.4,
};

export default spacing;
