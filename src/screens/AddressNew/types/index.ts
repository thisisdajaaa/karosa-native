/**
 *
 * @format
 *
 */

export type PropsType = {
  details: string;
  latitude: number;
  longitude: number;
};

export type AddressNewProps = {
  AddressLocation: {
    details: string;
    latitude: number;
    longitude: number;
  };
};
