import { LocationState } from "./models";

export const initLocationState: LocationState = {
  regionResponse: {
    response: [
      {
        id: 0,
        name: "",
      },
    ],
    isLoading: false,
  },
};
