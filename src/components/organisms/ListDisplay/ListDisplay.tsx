/**
 *
 * ListDisplay
 * @format
 *
 */

import React, {FC} from 'react';

import ListDisplayConfig from './config';
import type {PropsType} from './types';
import ListDisplayStyles from './styles';


const ListDisplay: FC<PropsType> = (props) => {
  const {} = props;

  return ;
};

export default ListDisplay;

