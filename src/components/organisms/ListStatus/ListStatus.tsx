/**
 *
 * ListStatus
 * @format
 *
 */

import React, {FC} from 'react';

import ListStatusConfig from './config';
import type {PropsType} from './types';
import ListStatusStyles from './styles';


const ListStatus: FC<PropsType> = (props) => {
  const {} = props;

  return ;
};

export default ListStatus;

