/**
 *
 * FormPicker
 * @format
 *
 */

import React, {FC} from 'react';

import FormPickerConfig from './config';
import type {PropsType} from './types';
import FormPickerStyles from './styles';


const FormPicker: FC<PropsType> = (props) => {
  const {} = props;

  return ;
};

export default FormPicker;

