/**
 *
 * @format
 *
 */

import { StyleSheet } from "react-native";

const FormPasswordStyle = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
  },
});

export default FormPasswordStyle;
