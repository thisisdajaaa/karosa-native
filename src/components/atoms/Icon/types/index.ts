/**
 *
 * @format
 *
 */

import { StyleProp, ViewStyle } from "react-native";

export type PropsType = {
  group: string;
  name: string;
  extraStyle: StyleProp<ViewStyle>;
  height: number;
  width: number;
};
