/**
 *
 * ShopAddress
 * @format
 *
 */

import React, {FC} from 'react';

import ShopAddressConfig from './config';
import type {PropsType} from './types';
import ShopAddressStyles from './styles';


const ShopAddress: FC<PropsType> = (props) => {
  const {} = props;

  return ;
};

export default ShopAddress;

